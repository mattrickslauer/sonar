// The Twitter/X card reuses the Open Graph image verbatim — same 1200×630
// radar card — so there's only one design to maintain.
export { default, alt, size, contentType } from "./opengraph-image";
